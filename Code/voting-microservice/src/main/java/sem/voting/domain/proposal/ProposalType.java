package sem.voting.domain.proposal;

/**
 * Enum to represent the type of proposal being used.
 * This is needed to inform on which behaviour the proposal should take.
 */
public enum ProposalType {
    BoardElection,
    HoaRuleChange
}
